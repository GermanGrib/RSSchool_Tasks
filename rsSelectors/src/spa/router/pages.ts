import { Pages } from '../../models/interfaces';

const Pages: Pages = {
  INDEX: 'index',
  PRODUCT: 'product',
  NOT_FOUND: 'not-found',
};

const ID_SELECTOR: string = '{id}';

export { Pages, ID_SELECTOR };
