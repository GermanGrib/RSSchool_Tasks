const headerClasses = {
  HEADER: ['header'],
  NAV: ['navigation'],
};

const namePages: { [key: string]: string } = {
  INDEX: 'Garage',
  WINNER: 'Winner',
};

export { headerClasses, namePages };
